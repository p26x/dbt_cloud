models:
  - name: bronze_bookings
    config:
      materialized: table
      schema: bronze
      
    columns:
      - name: BOOKING_AMOUNT
        tests:
          - booking_amount_min:   # generic data-test 
              arguments:
                warn_if: "<=10000"
                error_if: ">=30000"
          - not_null:
              arguments:
                severity : warn
                #severity : warn #error
